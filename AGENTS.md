## 🚀 Цель проекта

Здесь разрабатывается и тестируется функция умной сегментации латышских текстов. Цель – заменить простое разбиение текста по регулярному выражению `/(?<=[.!?])\s+/` на более продвинутый алгоритм, который корректно обрабатывает даты, заголовки, сокращения, списки, адреса и избегает ошибок сегментации (например, разрыв предложения из-за переноса строки посередине).

---

## 🧪 Как запустить и отлаживать проект

### 🔧 Установка и запуск

#### 1. Установка

```bash
git clone https://github.com/nn1k4/latvian_sentence_tester.git   # Клонирование репозитория
cd latvian_sentence_tester                                       # Переход в папку проекта
npm install                                                     # Установка npm-зависимостей
```

#### 2. Запуск

```bash
cd client              # Переход в папку клиента
npm install            # Установка зависимостей (однократно)
npm run dev            # Запуск клиента в режиме разработки
```

После запуска в консоли появится сообщение вида:

```text
  ➜  Local:   http://localhost:5174/
```

Затем откройте в браузере страницу [http://localhost:5174/](http://localhost:5174/) – отобразится интерфейс тестового приложения (React) для проверки алгоритма сегментации.

### 🧪 Тестирование

Для проверки корректности работы алгоритма и отсутствия ошибок используйте юнит- и интеграционные тесты:

```bash
# Запуск unit-тестов (Jest)
npm test

# Открытие E2E-тестов (Cypress) в интерактивном режиме
npm run cypress:open

# Прогон E2E-тестов (Cypress) в headless-режиме
npm run cypress:run
```

Unit-тесты проверяют функцию сегментации на отдельных строках, а E2E-тесты запускают весь интерфейс и сценарии, имитируя ввод текста и визуальную проверку результатов.

### ⚙️ Качество кода

Проект настроен на строгий контроль качества и стиля кода. Используются линтер и форматтер, а также проверка TypeScript-типов:

```bash
# Проверка линтером (ESLint) с выводом в консоль
npm run lint -- --format codeframe

# Проверка типизации TypeScript
npm run type-check

# Автоформатирование кода (Prettier)
npm run format

# Исправление проблем, найденных линтером
npm run lint:fix
```

Эти команды помогают поддерживать единый стиль кодовой базы и избежать распространённых ошибок. Рекомендуется запускать их перед коммитом изменений.

---

## 💻 Стандарты кодирования

- **Строгая типизация TypeScript**: Включён режим `strict`, поэтому весь код должен быть полностью типизирован. Избегайте использования `any` и приведения типов без необходимости – вместо этого определяйте интерфейсы и типы для данных. Благодаря строгой типизации многие ошибки ловятся на этапе компиляции.

- **ESLint и Prettier**: Настроены линтер ESLint (с рекомендованными правилами для JS/TS и React) и форматтер Prettier. ESLint интегрирован с Prettier, что исключает конфликты стиля. Код автоматически форматируется (2 пробела отступ, точка с запятой в конце выражений, двойные кавычки и пр.) и проверяется на соответствие стандартам. При разработке придерживайтесь этих автоформатированных стилевых правил – это обеспечивает единообразие кода.

- **Единый стиль и соглашения**: Имена переменных и функций — в camelCase, типы и интерфейсы — в PascalCase. Файлы с React-компонентами — в PascalCase (`MyComponent.tsx`), утилиты — в camelCase (`textUtils.ts`). Все новые функции должны сопровождаться комментариями, объясняющими их назначение, особенно для нетривиальной логики. Проект ориентирован на ES2020+ и современные возможности TypeScript/React, без устаревших подходов.

Придерживаясь этих стандартов, вы облегчаете чтение и поддержку кода, а также упрощаете сотрудничество – любой участник сможет быстро понять и внести изменения в проект.

---

## 🏗️ Структура проекта

```text
latvian_sentence_tester/
├── 📄 package.json              # Зависимости и скрипты проекта
├── 📄 vite.config.ts            # Конфигурация сборки (Vite)
├── 📄 tsconfig.json             # Настройки компилятора TypeScript (strict-mode)
├── 📄 cypress.config.ts         # Конфигурация E2E тестов Cypress
├── 📄 jest.config.js            # Конфигурация unit-тестов Jest
│
├── 📁 client/src/               # Исходный код фронтенда (React + TS)
│   ├── 🧠 utils/latvianSegmentation.ts    # Основной модуль алгоритма сегментации
│   ├── ⚛️ App.tsx                         # React-компонент: интерфейс тестирования сегментации
│   ├── 📄 main.tsx                        # Точка входа фронтенда
│   └── 🎨 index.css                       # Глобальные стили (Tailwind CSS)
│
└── 📁 client/cypress/e2e/       # E2E-тесты Cypress
    └── 🧪 main.cy.ts            # Основной сценарий end-to-end тестирования
```

Основные файлы и папки:

- **latvianSegmentation.ts** – содержит реализацию функции `splitIntoSentencesAdvanced` и вспомогательных функций для умной сегментации текста.
- **App.tsx** – позволяет вводить текст, запускать алгоритм сегментации и видеть результат (каждое предложение подсвечено отдельным цветом). Используется для визуального тестирования.
- **main.cy.ts** – скрипт Cypress, имитирующий ввод тестовых текстов и проверяющий, что сегментация работает правильно.
- Конфигурационные файлы (Vite, Jest, Cypress, TypeScript) – обеспечивают правильную сборку, тестирование и запуск проекта.

Такая структура отражает разделение ответственности: логика сегментации вынесена в модуль utils, UI для тестирования – в компонент App, а автотесты – в соответствующие директории.

---

## 🔄 Внесение изменений

Алгоритм сегментации рассчитан на различные случаи, но при обнаружении новых проблем или примеров, где он ошибается, следует внести правки. Ниже описан рекомендуемый порядок действий на примере гипотетического бага с фразой

\_«Anita ieskrien mājā un

aizsteidzas uz savu istabu.»\_

_«Anita ieskrien mājā un…»_ (предложение оказалось разорвано переносом строки после _"mājā"_, что неверно):

1. **Выявление проблемы и создание теста**: Сначала воспроизведите проблему в среде разработки **latvian_sentence_tester**. Добавьте unit-тест в `client/src/utils/__tests__/latvianSegmentation.test.ts` или расширьте E2E-тест, иллюстрируя некорректную сегментацию. Например, убедитесь, что фраза `"Anita ieskrien mājā un izlec ārā."` неправильно разбивается, если в исходном тексте стоит перенос строки после "mājā".
2. **Правка алгоритма**: Внесите изменения в функцию `splitIntoSentencesAdvanced` или связанные вспомогательные функции. В случае с _"mājā un..."_ причиной ошибки является разрыв строки внутри предложения. Возможное исправление: добавлять логику слияния строк, если строка заканчивается без знака препинания, а следующая начинается со строчной буквы (например, _"un"_ – союз с маленькой буквы, что указывает на продолжение предыдущего предложения). Для этого можно предварительно склеивать такие строки перед сегментацией или модифицировать обработку в коде (например, в функции `processSentencesInLine`). Если проблема связана с некорректным определением сокращения или другого шаблона, возможно, потребуется дополнить списки аббревиатур, географических обозначений и т.п.
3. **Обновление тестов**: После правки убедитесь, что новый тест теперь проходит. Запустите `npm test` для проверки unit-тестов и `npm run cypress:run` для прогона всех интеграционных тестов. Проверьте, что изменения не нарушили другие кейсы (отсутствуют регрессии). Если какие-то существующие тесты падают – проанализируйте причину: либо допущена регрессия (сломалось то, что раньше работало) и алгоритм требует доработки, либо изменилось ожидаемое поведение и нужно обновить сами тесты.
4. **Документирование**: При внесении существенных изменений обновите документацию, если необходимо. Например, если добавлено новое правило обработки или поддержка нового типа сокращений, отразите это в **Codex.md** (подробном описании алгоритма) для будущих разработчиков, указав, почему потребовалось это изменение и как оно реализовано.

---

## 🔍 Рекомендации по тестированию сложных случаев

Алгоритм сегментации покрывает множество сценариев, поэтому важно тщательно проверять **краевые случаи** – нетипичные или пограничные примеры, где легко допустить ошибку. Рекомендуется:

- **Различные категории ввода**: Составьте тесты для каждого типа случаев:
  - Предложения с **датами**: например, _"Šodien ir 15. novembrī."_ (не должно разбиться после "15.").
  - С **аббревиатурами**: _"Dr. Kalniņš strādā klīnikā."_, _"Viņi darīja u.c. lietas."_ (не должно разделиться после "Dr." или "u.c.").
  - С **адресами**: _"Viņš dzīvo 5. stāvā."_, _"Nosūtīt vēstuli uz Brīvības ielā 10. dzīvokli 5."_ (точка после числа не должна завершать предложение).
  - **Нумерованные списки**: _"1. Pamosties. 2. Iet skriet."_ – если элементы написаны подряд или через перенос, убедиться, что "1." и "2." не трактуются как конец предложения.
  - **Заголовки**: отдельные короткие фразы без точки (например, "ZIŅAS", "Svarīgi Jaunumi") должны оставаться цельным предложением, а не присоединяться к следующему.

- **Комбинации случаев**: Используйте сложные тексты, где присутствуют сразу несколько упомянутых элементов. Например, текст: _"ZIŅAS\nDr. Bērziņš ieradās 2023. gadā. Viņš dzīvo 3. stāvā. Skat. 1. pielikumu."_ – здесь есть заголовок, сокращение с точкой, год с точкой, адресный элемент и нумерация "1." в конце. Убедитесь, что алгоритм правильно сегментирует каждую часть:
  1. `"ZIŅAS"` отдельно как заголовок.
  2. `"Dr. Bērziņš ieradās 2023. gadā."` как единое предложение (точка в "Dr." и после "2023." не рвут предложение).
  3. `"Viņš dzīvo 3. stāvā."` как единое предложение (не разделилось на "3." и "stāvā").
  4. `"Skat. 1. pielikumu."` – убедиться, что не произошло разбиения перед "1." или после него, вся фраза осталась цельной.

- **Переносы строк внутри предложений**: Протестируйте случаи, когда в исходном тексте одно предложение разбито случайным переносом строки. Как обсуждалось, _"Anita ieskrien mājā\nun izlec ārā."_ должно распознаться как одно предложение, несмотря на внутренний перенос строки. Текущая реализация по умолчанию разделяет текст по переносам строк, поэтому такая ситуация может привести к неверной сегментации. Если подобный кейс актуален, добавьте тест на него и рассмотрите доработку алгоритма (например, объединение строк, оканчивающихся без знака конца предложения, с последующей строкой).
- **Прямая речь и кавычки**: Проверьте случаи с диалогами и цитатами, особенно если фраза автора разбита или содержит многоточие. Например:
  _"„Tavu mammu... \nViņa nomira,” saka skolotāja."_ – здесь прямая речь разбита на две строки (после многоточия и перед продолжением с заглавной буквы). Алгоритм не должен ошибочно разделить такую фразу на два предложения. Если диалоговые реплики могут находиться на разных строках или содержать паузы с многоточиями, убедитесь, что сегментация учитывает открывающие и закрывающие кавычки и не разрывает предложение внутри реплики.
- **Автоматизация тестирования**: Каждый новый особый случай, выявленный в процессе использования, стоит добавлять в автотесты. Напишите Jest-тесты для функции `splitIntoSentencesAdvanced`, задавая входной текст и ожидаемый массив предложений. Для интерактивной проверки пользуйтесь UI приложения: вводите проблемный текст в интерфейс и смотрите, где подсветка сегментов отображает неправильный разрыв или слияние.

Следуя этим рекомендациям, вы сможете уверенно развивать алгоритм, зная, что все известные краевые случаи находятся под контролем. При добавлении нового правила сегментации всегда думайте, не повлияет ли оно на уже обработанные случаи, и расширяйте набор тестов по мере роста функциональности. Это значительно упростит сопровождение проекта и интеграцию модуля в другие приложения.

_(последнее обновление: 13 августа 2025)_
